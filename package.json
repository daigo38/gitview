{
  "name": "gitview-server",
  "version": "1.0.0",
  "description": "Local Git repository viewer server",
  "type": "module",
  "module": "server.ts",
  "scripts": {
    "start": "bun run server.ts",
    "dev": "bun --watch server.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.6.14",
    "mime": "^4.0.4"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^6.0.3"
  }
}
